{
  "name": "the42laws-atlas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-atlas.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test tests/*.test.cjs tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "marked": "18.0.12"
  }
}
